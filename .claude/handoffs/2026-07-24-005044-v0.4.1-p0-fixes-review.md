# Handoff: project-insight v0.4.1 P0 Fixes — 审查后修复报告

> 基于 v0.4.0 code review 报告，按 spec-v0.4.1-p0-fixes.md 完成的 3 个 P0 阻塞修复 + P1 级清理。

## Session Metadata

- **Created**: 2026-07-24 00:50
- **Project**: `E:\project\project-insight`
- **Current Version**: v0.4.0 → **v0.4.1** (未提交)

## Handoff Chain

- **Continues from**: `2026-07-24-003444-v0.4.0-implementation-review.md`（实施 handoff）
- **Input references**:
  - `C:\Users\song\WorkBuddy\2026-07-24-00-38-02\outputs\v0.4.0-code-review-report.md` — 4 轴审查报告（45 项发现）
  - `docs/planning/spec-v0.4.1-p0-fixes.md` — P0 修复规格说明

---

## 修复清单

### P0 — 阻塞级修复（3 项）

| Fix | 文件 | 问题 | 修复 |
|:---|:---|:---|:---|
| **#1** | `env_vars.py:16-23` | 重复 pattern（`environ.get` 与 `os.environ.get` 自匹配）+ `[\s*'"]` 字符类语义错误 | 合并 6 条 pattern 为 4 条，修复 `[\s*'"]` → `\s*['"]`，统一加 `(?:os\.)?` 前缀 |
| **#2** | `imports.py:47` + `local_graph.py:64-113` | `scan_imports` 截断完整 import 路径只留根名 → broken/local 判定整体失准 | 新增 `scan_imports_full()` 返回完整路径；`local_graph.py` 改用完整路径匹配（精确→前缀→根名三级回退） |
| **#3** | `file_refs.py:25` | `Path("file.py")` 漏报——不匹配 `pathlib.Path("file.py")` | `Path` → `(?:pathlib\.)?Path` |

### P1 — 提交前修复（6 项）

| ID | 文件 | 问题 | 修复 |
|:---|:---|:---|:---|
| S1 | `project_insight.py:170-186` | `_append_tree` 与 `tree.py` 完全重复（17 行代码） | 删除 `_append_tree` + `_format_generic` 中的 project_tree 分支（tree 模块已自带 format_plain） |
| S2 | `env_vars.py:29,80,90` | 三处自行 `read_text(try/except)`，未用 `safe_read()` | 全部改为 `content = safe_read(filepath)` |
| S3 | `env_vars.py:13` | `should_skip` 已导入但从未调用 | 从 import 中删除 |
| S4 | `local_graph.py:133` | `format_plain()` 缺少 docstring | 新增 `"""将本地模块依赖图格式化为纯文本输出"""` |
| S5 | `project_insight.py:28` | `main()` 缺少 docstring | 新增 docstring + `-> None` 返回类型 |
| S6 | `project_insight.py:126` | `_print_plain` 缺少返回类型标注 | 补充 `-> None` |

### P1 — 逻辑修复（1 项，合并实施）

| ID | 文件 | 问题 | 修复 |
|:---|:---|:---|:---|
| L10 | `local_graph.py:133` | `format_plain` 不显示 `broken_imports` | 新增 broken_imports 渲染逻辑，断裂信息在 plain 格式中可见 |

### 测试更新

| 文件 | 变更 |
|:---|:---|
| `test_project_insight.py` | `test_format_generic_returns_string` 和 `test_format_generic_project_tree` 更新为断言空字符串（_format_generic 不再处理 project_tree，已由 tree.format_plain 覆盖） |

---

## 验证结果

```
116 passed, 1 failed (pre-existing test_print_plain — model_refs bug, 非本版引入)
所有 P0/P1 修复未引入新破坏
```

### 分步验证

| 阶段 | 模块 | 结果 |
|:---|:---|:---|
| Fix #1 后 | `test_env_vars.py` | 17/17 passed |
| Fix #2 后 | `test_local_graph.py` + `test_imports.py` | 18/18 passed |
| Fix #3 后 | `test_file_refs.py` | 11/11 passed |
| 全量回归 | 全量 | 116/116 passed |

---

## Git 状态

```
Branch: main
Up to date with: origin/main
工作区: Modified (未暂存, 未提交)
```

相较于 v0.4.0 实施 handoff 后的变更：
```diff
 M scripts/extractors/env_vars.py   — 3 处改动 (pattern合并 + safe_read + 删死导入)
 M scripts/extractors/imports.py    — +scan_imports_full()
 M scripts/extractors/local_graph.py — 2 处改动 (完整路径匹配 + format_plain broken显示)
 M scripts/extractors/file_refs.py  — 1 处改动 (pathlib.Path支持)
 M scripts/project_insight.py       — -_append_tree + docstring + 类型标注
 M tests/test_project_insight.py   — 测试适配
```

---

## 审查模型确认要点

1. **Fix #1 验证**：合并后是否仍有未预期的 `environ.get`/`os.environ` 子串匹配？6→4 条 pattern 是否覆盖原始用例？
2. **Fix #2 验证**：`scan_imports_full()` 保留完整路径后，`local_graph.py` 的三级匹配策略（精确→前缀→根名）是否覆盖所有项目内 import 情况？
3. **S1 安全性**：删除 `_append_tree` 后，`tree` 模块的 `format_plain` 是否能完全承接其渲染职责？

---

**Security Reminder**: 本 handoff 文件不包含任何 API Key、Token 或密码。
